# ~/.zshenv
# The ONLY zsh file that lives at ~/
# ZSH loads this first, for every shell (scripts, SSH, interactive, login)
# One job: redirect everything else to $ZDOTDIR

export ZDOTDIR="$HOME/.config/zsh"
